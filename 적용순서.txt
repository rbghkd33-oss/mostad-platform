모스트애드 v14 권한·직원업무 시스템 적용순서

1) patch 안의 app, supabase 폴더를 기존 mostad-platform 폴더에 덮어쓰기
2) Supabase SQL Editor에서 supabase/migrations/004_roles_staff_workflow.sql 전체 실행
3) SQL Editor에서 본인 계정을 최고관리자로 변경
   update public.profiles set role='super_admin' where email='본인이메일';
4) 로그아웃 후 다시 로그인
5) 로컬 확인: pnpm dev
   관리자: http://localhost:3000/admin
   직원: http://localhost:3000/staff
6) Git 배포
   git add .
   git commit -m "최고관리자 직원 권한 및 업무배정 시스템 추가"
   git push
