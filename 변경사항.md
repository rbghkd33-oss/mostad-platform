# 모스트애드 인스타 자동화 실시간 연동 v34

## 홈페이지
- 사용자 소유권을 확인하는 Next.js 서버 프록시 API 추가
- 브라우저에 `X-API-Key`가 노출되지 않도록 서버에서만 자동화 API 호출
- 가동 중 계정의 좋아요·팔로우·댓글·스토리 진행률 표시
- 최근 작업 로그 최대 8개 화면 표시, 서버에서는 최대 30개 반환
- 4초 간격 자동 갱신
- 관리자 승인 시 암호화 비밀번호를 서버에서 복호화한 뒤 자동화 서버로 계정 등록
- 승인 순서: 계정 등록 → 설정 동기화 → 자동화 시작 → Supabase 30일 승인 처리
- 자동화 연동 실패 시 승인 상태를 변경하지 않으며 시작한 자동화를 정지·정리

## Python 자동화 서버
- 홈페이지 전용 보안 관리 API 추가
  - `POST /api/external/admin/accounts/add`
  - `POST /api/external/admin/accounts/{insta_id}/settings`
  - `POST /api/external/admin/accounts/{insta_id}/start`
  - `POST /api/external/admin/accounts/{insta_id}/stop`
  - `DELETE /api/external/admin/accounts/{insta_id}`
- 위 경로는 모두 `X-API-Key` 인증 필수
- 기존 로컬 관리 API는 유지
- localhost 및 127.0.0.1의 임의 포트를 허용해 로컬 UI CORS 400 문제 완화

## 검증
- Python `py_compile` 검사 통과
- 변경된 TypeScript/TSX 파일 구문 변환 검사 통과
- 실제 Vercel 빌드 및 자동화 실행은 사용자의 환경변수와 Supabase/터널 연결 상태에서 최종 확인 필요
